---
declare interface Props {
    className?: string;
}

const { className = '' , ...rest} = Astro.props as Props;

const completeClassName = `uk-container ${className}`.trim();

---

<div class={completeClassName}, {...rest}>
    <slot />
</div>