---
import Navbar from '../components/navbar/Navbar.astro';
import OffCanvas from '../components/offCanvas/offCanvas.astro';
interface Props {
	title: string;
}

const { title } = Astro.props;
---
<script>
	import { UIkit, Icons } from "franken-ui/uikit/js/dist";
	UIkit.use(Icons);
	// @ts-ignore
	window.UIkit = UIkit;
</script>

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Astro description" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<title>{title} | Conference on End-of-Life</title>
	</head>
	<body>
		<Navbar />
		<slot />
		<aside>
			<OffCanvas />
		</aside>
	</body>
</html>

<style is:global>
	@tailwind base;
	@tailwind components;
	@tailwind utilities;
</style>
